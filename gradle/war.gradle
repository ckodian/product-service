apply plugin: "war"

bootWar {
    mainClassName = "au.twc.core.product.ProductsvcApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
